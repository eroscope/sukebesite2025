# Codexハイブリッド記事生成 監査と実装方針

更新日: 2026-08-22

## 目的

記事の意味判断と文章品質はCodexへ戻す。ただし、URL取得、素材回収、重複排除、
動画検査、保存、公開、巡回、ログまでCodexへ任せると、利用量と失敗時の損失が
大きくなる。そのため、意味を理解しないと品質が出ない工程だけを1記事1回の
Codex処理へ集約し、それ以外は通常プログラムで確定的に処理する。

## 旧方式の実測

`.article-studio/chatgpt-primary-queue.json` の500件を集計した結果:

| 状態 | 件数 |
| --- | ---: |
| 失敗 | 216 |
| 利用制限などによる停止 | 151 |
| 完成 | 91 |
| 重複整理 | 28 |
| 対象外 | 14 |

主な失敗記録:

| 原因 | 件数 | 判定 |
| --- | ---: | --- |
| ChatGPT画面の利用制限・送信停止 | 209 | ブラウザUI方式を廃止すれば解消 |
| `NoneType` など取得結果の形式崩れ | 49 | 事前検査と型の正規化で解消 |
| ChatGPT画面の返答待ち90秒超過 | 35 | `codex exec` 直接実行で解消 |

2026-08-15は、完成1件に対して失敗54件、対象外13件、停止39件だった。文章の
精度だけでなく、同じ候補に取得、判定、生成を何度も実行する構造が利用量を
消費していた。

## 現在の1記事フロー

1. プログラムがURLを正規化し、重複と危険なURLを確認する。
2. FANZAは公式商品情報、Xはログイン済みブラウザ、一般Webはレンダリング後の
   DOM・通信と静的HTMLの両方から素材を回収する。
3. プログラムが同一画像、広告URL、壊れた画像、再生できない動画を事前除外する。
4. 明確な中継ページはリンク文と同一ホストを使ってプログラムだけで本編へ進む。
5. 本文、ページ全景、候補画像、動画フレーム、DOM位置をCodexへ1回だけ送る。
6. Codexが成人向け判定、記事本編と広告の判別、採用素材、タイトル、概要、
   記事固有タグ、自然な5ch風レスを1つの構造化JSONで返す。
7. プログラムがCodexの素材IDを実ファイルと照合し、重複配置を除去する。
8. プログラムがサムネイル、動画、画像、レスを記事形式へ組み、公開可能性を検査する。
9. 保存、公開待機、公開更新、履歴、失敗学習はすべてプログラムが行う。

## 工程別の担当

| 躓いていた部分 | Codexで解決するか | 最終担当 | 理由 |
| --- | --- | --- | --- |
| 広告画像を本文画像として選ぶ | する | Codex | 画面位置と記事主題の意味理解が必要 |
| サムネイルだけの画像を本文へ重複配置 | 一部 | Codex判定 + プログラム | 意味判定後、配置重複はIDで確実に防ぐ |
| 動画を静止画扱いする | 一部 | プログラム + Codex | 動画取得・再生確認はプログラム、内容判定はCodex |
| JavaScriptで追加された2本目以降の動画を落とす | しない | プログラム | ブラウザ通信結果と静的HTMLの結合不具合だった |
| タイトルが画像と合わない | する | Codex | 画像・本文・枚数を同時に理解させる |
| 1枚なのに「まとめ」と書く | 一部 | Codex + プログラム | Codexへ枚数を渡し、最終タイトルも枚数ルールで補正 |
| レスが支離滅裂、第三者目線と投稿者目線が混ざる | する | Codex | 記事全体の会話整合と推敲が必要 |
| タグにPR、FANZA、成人向けなどが混ざる | 一部 | Codex + プログラム | 固有タグを作らせ、禁止タグは保存前に除去 |
| 関係ないFANZA商品を記事へ付ける | 一部 | Codex + プログラム | 人物・品番の根拠をCodexで判定し、URL照合はプログラム |
| 本編への中継リンクをたどれない | 一部 | 原則プログラム | 明確な導線は無料で追跡し、曖昧な場合だけ同じCodex回答を使う |
| 画像・動画そのものを取得できない | しない | プログラム | AIは存在しないファイルや拒否された通信を復元できない |
| 動画が保存後に再生できない | しない | プログラム | MIME、HTTP応答、実ファイル、公開パスの検査が必要 |
| 同じURLを何度も処理する | しない | プログラム | 正規化URLと履歴で重複排除する |
| 自動と手動で設定や同時数が違う | しない | プログラム | 両方を同じ待機列・同じ1件処理へ統一する |
| 利用制限で同じ候補を再送し続ける | しない | プログラム | 上限時は候補を停止し、新規送信を止める |
| 保存・公開時のブロック数不一致 | しない | プログラム | JSONスキーマと実ファイルを保存前に検証する |
| 著作権・掲載許可の有無 | しない | 人の確認 | AIは許諾の事実を保証できない |

## Codex使用量を抑える規則

- 1記事につきCodexは原則1回。旧方式の「解析、生成、推敲」の別送信をしない。
- 素材なし、重複、壊れたURL、明確な対象外はCodexへ送る前に止める。
- ページ取得の再試行にCodexを使わない。
- 明確な中継ページの移動にCodexを使わない。
- 画像・動画のダウンロード、画素重複、再生確認にCodexを使わない。
- Codexが返したJSONは再質問せず、プログラムで修正可能なID重複や禁止タグを補正する。
- サイト別に成功した取得方法を保存し、次回はその方法を先に使う。
- 旧ChatGPT画面操作は通常経路から外し、保存済みのCodexアプリ認証を直接利用する。
- 記事生成だけは `gpt-5.6-luna` / reasoning `high` に固定し、Codexアプリ全体の
  既定モデル設定は変更しない。

## 現在確認できた改善

- 一般Web、X、FANZAを同じ手動・自動処理へ統一した。
- 複数URL追加はFANZA専用ではなく、すべての `http://` / `https://` URLを受け付ける。
- 自動巡回も手動実行も同時処理数は1件で、同じ `CodexSendWorker` を使う。
- `chaos-giga.com/archives/287311` は事前回収で画像2件を取得した。
- `chaos-giga.com/archives/287395` はブラウザ通信にだけ存在した2本目の動画を含め、
  動画2件と両方の確認フレームを取得した。
- 完成記事の画像・動画IDは保存前に再照合し、広告判定済み素材を本文へ置かない。
- 旧学習データにあった「媒体情報なしの完成行を画像0・動画0の成功として学習する」
  処理を廃止した。
- 7枚以上のギャラリーは、保存用原本を全枚維持したまま、Codex向けには先頭画像と
  ID付き一覧シートへ圧縮する。18枚の商品で添付18ファイルを3ファイルへ削減した。
- 圧縮前に12分で時間切れしたFANZA商品が、圧縮後はCodex 1回、262.8秒で完成し、
  公式画像18枚を維持したまま `adult_approved` まで通過した。
- FANZA動画は商品ごとの公式提供を確認する。`mida00489` は公式サンプル動画1本を
  取得し、`sivr00390` はFANZA無料動画ツール自体が404のため動画なしと判定した。
- 記事内PRは、Codexが画像・動画ごとに特定したFANZA品番を公式検索結果の品番と
  完全一致で再検証する。品番が一致しない候補や、単に同じページ内にあった広告は採用しない。
- 画像に対応する商品はその画像群の直下、動画に対応する商品はその動画の直下へ置く。
  同じ商品を複数素材が指す場合は重複掲載せず、動画側を優先する。
- 記事素材と完全一致する商品がない場合、無関係なFANZA商品を自動挿入しない。
  記事末尾には主題との関連スコアが正の内部関連記事だけを表示する。
- PRプレビューには配置先、一致度、一致根拠を表示し、公開HTMLにも監査用属性を残す。
- 保存済み記事の旧PRも読込時に再判定する。記事元FANZA品番と一致するものだけを
  画像・動画の直下へ移し、根拠情報のない旧おすすめは表示・再保存から除外する。
- FANZAのPRは、記事作成時に商品URLだけを下書きへ保存し、プレビュー・公開時に
  現在設定中のアフィリエイトIDから `al.dmm.com` の広告リンクを自動生成する。
- アフィリエイトIDは管理サイトごとに最初の1回だけ登録する。手動作成、自動巡回、
  再生成、編集、公開の全経路が同じ変換処理を使い、記事ごとのリンク作成は不要。
- コピー元に他人の `af_id` が含まれていても除去して自分のIDへ置換する。ID未設定、
  または商品URLを確認できないPRはクリック不能にし、そのまま公開できない。
- 設定画面の「保存して全PRに適用」で、既存公開記事のFANZA商品ボタンも一括更新する。
- 巡回先は7日ごとに検索結果と情報源ハブから新規候補を探し、実記事URL、成人向け判定、
  画像・動画数、通販・未成年表現を事前検査して、合格したサイトだけを最大2件追加する。
- 同じサイトへの偏りを避けるため最終選択時刻の古い情報源を優先し、自動追加サイトが
  記事生成に3回連続で失敗した場合は自動停止する。追加・不合格理由は情報源画面に残す。
- 専用Python環境で全274テストが成功した。

## 残る限界

- 元サイトがアクセスを拒否した素材、期限切れURL、DRM、ログイン権限外の媒体は取得できない。
- FANZA商品に公式サンプル動画が提供されていない場合、画像だけの記事になる。
- Codexにも利用上限はある。上限到達時は記事を失敗扱いで連打せず停止する。
- 画像だけで人物名、場所、年齢などを断定できない場合は、ページ本文の根拠を優先する。
- Codexの判断は最終的な掲載許可や法的保証ではない。公開前の許可管理は別工程として残す。
